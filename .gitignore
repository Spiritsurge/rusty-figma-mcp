# Rust
/target/
**/*.rs.bk

# Node / plugin build
node_modules/
plugin/dist/
plugin/.vite/

# Editor / OS
.DS_Store
._*
.idea/
.vscode/

# Local session state written at runtime
.hostlink/

# Local MCP client config — absolute paths, per machine
.mcp.json

# Git hooks. Local to the machine, not part of building or running this.
.githooks/
