* text=auto eol=lf

# Executables must keep LF regardless of platform: a shell script with CRLF
# fails on Linux and macOS with a confusing "bad interpreter" error.
*.sh            text eol=lf

*.png binary
*.pdf binary
